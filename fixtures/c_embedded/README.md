# Embedded SQLite fixture

This directory is reserved for the validation matrix. A pinned official
SQLite amalgamation and symbol-visibility variants belong to a later phase;
Fase 0/1 deliberately does not download or compile `sqlite3.c`.
